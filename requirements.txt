# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# ML & AI - Core (All Platforms)
transformers>=4.45.0
sentence-transformers>=3.2.0
huggingface-hub>=0.26.0
einops>=0.8.0

# PyTorch - Platform Specific
# For NVIDIA GPU (Linux): Install with CUDA support
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For Mac (Apple Silicon): Install regular torch
# pip install torch torchvision torchaudio
# For CPU only: Install regular torch
torch>=2.3.0

# MLX - Apple Silicon Only (Optional)
# Only install on Mac with Apple Silicon
# pip install mlx>=0.20.0 mlx-lm>=0.19.0
# Commented out for cross-platform compatibility:
# mlx>=0.20.0
# mlx-lm>=0.19.0

# Text Processing
langchain>=0.3.7
langchain-community>=0.3.6

# Web Search for Hybrid RAG
tavily-python>=0.3.0

# Redis
redis>=5.0.0
redis-om>=0.2.1

# Utilities
python-dotenv>=1.0.0
pydantic>=2.7.4
pydantic-settings>=2.1.0
numpy>=1.26.0
tqdm>=4.66.0
requests>=2.31.0

# Logging
loguru>=0.7.2

# Optional: Accelerate for better model loading on GPU
accelerate>=0.33.0

# Authentication & Security (v2.3.0)
python-jose[cryptography]>=3.4.0  # JWT token handling - Updated to fix PYSEC-2024-232, PYSEC-2024-233
passlib[bcrypt]==1.7.4  # Password hashing
email-validator==2.1.0  # Email validation
slowapi==0.1.9  # Rate limiting
Pillow>=10.4.0  # CAPTCHA image generation - Updated for security
pyotp>=2.9.0  # TOTP (Google Authenticator) support
qrcode>=7.4.0  # QR code generation for 2FA setup
ecdsa>=0.19.0  # Explicit version for python-jose dependency - CVE-2024-23342 mitigation
clamd>=1.0.2  # ClamAV antivirus integration for file scanning

# Audio Processing (for TTS mixed-language support)
pydub>=0.25.0

# Testing (Development)
pytest-asyncio==0.23.0
httpx==0.27.0
