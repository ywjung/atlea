# Requirements for Linux (NVIDIA GPU)
# Install with: pip install -r requirements-linux.txt

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# ML & AI - Core
transformers>=4.45.0
sentence-transformers>=3.2.0
huggingface-hub>=0.26.0
einops>=0.8.0

# PyTorch with CUDA support
# Install separately with CUDA:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Or uncomment below for CPU-only version:
# torch>=2.3.0
# torchvision>=0.18.0
# torchaudio>=2.3.0

# Text Processing
langchain>=0.3.7
langchain-community>=0.3.6

# Utilities
python-dotenv>=1.0.0
pydantic>=2.7.4
pydantic-settings>=2.1.0
numpy>=1.26.0
tqdm>=4.66.0
requests>=2.31.0
loguru>=0.7.2
accelerate>=0.33.0

# CUDA-specific optimizations
# These will be used automatically if CUDA is available
bitsandbytes>=0.41.0  # For 4-bit/8-bit quantization on GPU
